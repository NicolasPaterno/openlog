[project]
name = "openlog-ai-worker"
version = "0.1.0"
description = "AI Worker for OpenLog - analyzes logs using LLMs"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 120
