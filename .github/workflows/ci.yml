name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  GO_VERSION: "1.22"
  PYTHON_VERSION: "3.11"

jobs:
  lint-go:
    name: Lint & Test - Ingestion (Go)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ingestion
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # TODO: descomentar quando o servico Go estiver implementado
      # - name: Download dependencies
      #   run: go mod download
      #
      # - name: Run linter
      #   uses: golangci/golangci-lint-action@v4
      #   with:
      #     working-directory: services/ingestion
      #
      # - name: Run tests
      #   run: go test -v -race -coverprofile=coverage.out ./...

      - name: Placeholder
        run: echo "Go CI pipeline ready - awaiting service implementation"

  lint-python:
    name: Lint & Test - AI Worker (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ai-worker
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # TODO: descomentar quando o servico Python estiver implementado
      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -r requirements.txt
      #     pip install ruff pytest
      #
      # - name: Lint with ruff
      #   run: ruff check src/
      #
      # - name: Run tests
      #   run: pytest -v

      - name: Placeholder
        run: echo "Python CI pipeline ready - awaiting service implementation"

  docker:
    name: Docker Build Check
    runs-on: ubuntu-latest
    needs: [lint-go, lint-python]
    steps:
      - uses: actions/checkout@v4

      # TODO: descomentar quando os Dockerfiles existirem
      # - name: Build ingestion image
      #   run: docker build -t openlog-ingestion:ci services/ingestion/
      #
      # - name: Build ai-worker image
      #   run: docker build -t openlog-ai-worker:ci services/ai-worker/

      - name: Placeholder
        run: echo "Docker build pipeline ready - awaiting Dockerfiles"
